# Jessica Lynn Suttles's Config
Inspired by [Ehren Murdick's Config](http://github.com/ehrenmurdick/config)

## Install everything:

`rake install:all`

## Or specific stuff:

`rake install:ack`
`rake install:all`
`rake install:git`
`rake install:irb`
`rake install:vim`
`rake install:zsh`
